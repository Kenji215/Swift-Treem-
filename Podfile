# Uncomment this line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'Treem' do
    
pod "AWSCore"
pod "AWSS3"
pod "CorePlot", :git => 'https://github.com/core-plot/core-plot.git', :branch => 'release-2.0'
pod "OAuthSwift"
pod "libPhoneNumber-iOS"
pod "Kingfisher"
pod "KMPlaceholderTextView"
pod "Locksmith"
pod "SwiftyJSON"
pod "SinchRTC"
pod "TTTAttributedLabel"

# use git, cocoa pod not up to date
pod "CLImageEditor/AllTools", :git=> 'https://github.com/yackle/CLImageEditor.git'

end

target 'TreemTests' do

end